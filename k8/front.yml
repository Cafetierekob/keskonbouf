apiVersion: apps/v1
kind: Deployment
metadata:
  name: keskonbouf-front
  labels:
    app: keskonbouf-front
spec:
  replicas: 3
  selector:
    matchLabels:
      app: keskonbouf-front
  template:
    metadata:
      labels:
        app: keskonbouf-front
    spec:
      containers:
      - name: front
        image: keskonbouf_front:latest
        imagePullPolicy: Never
        ports:
        -containerPort: 8501